# Music Genre / Instrument Classifier — Python dependencies
# Install with: pip install -r requirements.txt

# ── YAMNet backend ────────────────────────────────────────
tensorflow>=2.12,<3.0
tensorflow-hub>=0.14
resampy>=0.4.2

# ── musicnn backend ───────────────────────────────────────
musicnn>=0.1.0

# ── Audio I/O & processing (replaces MATLAB) ─────────────
librosa>=0.10
soundfile>=0.12
sounddevice>=0.4        # mic input only — safe to skip in Codespaces

# ── Visualisation ─────────────────────────────────────────
matplotlib>=3.7

# ── Shared ────────────────────────────────────────────────
numpy>=1.23,<2.0
